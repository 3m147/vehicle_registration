{
  "name": "vehicle-registration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/public dist/server dist/.openai && cp -R index.html css js template .nojekyll dist/public/ && cp .openai/hosting.json dist/.openai/hosting.json && cp server/index.js dist/server/index.js"
  }
}
